import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return <div>Hello This is the start of the comment section app project</div>
};

ReactDOM.render(<App />, document.querySelector('#root'))